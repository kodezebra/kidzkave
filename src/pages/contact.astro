---
import PhoneIcon from "../assets/icons/phone.svg";
import WhatsappIcon from "../assets/icons/whatsapp.svg";
import CallToAction from "../components/home/CallToAction.astro";
import PageHeader from "../components/shared/PageHeader.astro";
import Layout from "../layouts/Layout.astro";
---

<Layout
  title="Contact Us | Kids Kave Junior School"
  description="Contact Kids Kave Junior School in Mukono to learn more about admissions, daycare, nursery, and primary education."
>

  <PageHeader
  title="Get in Touch"
  subtitle={`Reach out to schedule a visit, ask about admissions, or learn more about
        life at Kids Kave Junior School.`}/>

  


  <!-- ================= CONTACT INFO + FORM ================= -->
  <section class="py-16 sm:py-20">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">

        <!-- LEFT: CONTACT DETAILS -->
        <div class="space-y-8">

          <!-- Visit -->
          <div class="flex gap-4">
            <div class="flex items-center justify-center w-10 h-10 rounded-lg bg-primary/10">
              <span class="text-primary font-bold">📍</span>
            </div>
            <div>
              <h2 class="font-heading text-xl font-bold text-heading">
                Visit Our School
              </h2>
              <p class="mt-1 text-text-muted">
                Mukono, Namumira – along Katosi Road<br />
                <span class="text-sm">(Approx. 10 mins from Mukono town)</span>
              </p>
            </div>
          </div>

          <!-- Call -->
          <div class="flex gap-4">
            <div class="flex items-center justify-center w-10 h-10 rounded-lg bg-secondary/10">
              <PhoneIcon class="w-5 h-5 text-secondary" />
            </div>
            <div>
              <h2 class="font-heading text-xl font-bold text-heading">
                Call Us
              </h2>
              <p class="mt-1 text-text-muted flex items-center gap-2">
                <WhatsappIcon class="w-4 h-4 text-green-500" />
                <a href="tel:0786501757" class="hover:text-primary block">
                  0786501757 (WhatsApp)
                </a>
              </p>
              <p class="mt-1 text-text-muted flex items-center gap-2">
                <PhoneIcon class="w-4 h-4 text-secondary" />
                <a href="tel:0756764701" class="hover:text-primary block">
                  0756 764 701
                </a>
              </p>
            </div>
          </div>

          <!-- Hours -->
          <div class="flex gap-4">
            <div class="flex items-center justify-center w-10 h-10 rounded-lg bg-accent/10">
              <span class="text-accent font-bold">⏰</span>
            </div>
            <div>
              <h2 class="font-heading text-xl font-bold text-heading">
                School Hours
              </h2>
              <p class="mt-1 text-text-muted">
                Monday – Friday<br />
                7:00 AM – 5:00 PM
              </p>
            </div>
          </div>
        </div>

        <!-- RIGHT: FORM -->
        <!-- <div class="rounded-2xl border border-border bg-bg-soft p-8">
          <h2 class="font-heading text-2xl font-bold text-heading">
            Send Us a Message
          </h2>
          <p class="mt-2 text-text-muted">
            Fill out the form below and we’ll get back to you shortly.
          </p>

          <form class="mt-6 space-y-4">
            <input
              type="text"
              placeholder="Your Name"
              class="w-full rounded-lg border border-border bg-bg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-primary/30"
            />
            <input
              type="email"
              placeholder="Your Email"
              class="w-full rounded-lg border border-border bg-bg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-primary/30"
            />
            <textarea
              rows="4"
              placeholder="Your Message"
              class="w-full rounded-lg border border-border bg-bg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-primary/30"
            ></textarea>

            <button
              type="submit"
              class="w-full rounded-lg bg-primary px-6 py-3 font-semibold text-white hover:bg-primary/90 transition"
            >
              Send Message
            </button>
          </form>
        </div> -->
      </div>
    </div>
  </section>

  <!-- ================= MAP ================= -->
  <section class="pb-16">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="rounded-2xl overflow-hidden border border-border shadow-sm h-[400px]">
        <iframe
          title="Kids Kave Junior School Location"
          src="https://www.google.com/maps?q=0.33516991778327465,32.76172552757652&hl=en&z=15&output=embed"
          class="w-full h-full border-0"
          loading="lazy"
          referrerpolicy="no-referrer-when-downgrade"
        ></iframe>
      </div>
    </div>
  </section>
  <CallToAction />
</Layout>

